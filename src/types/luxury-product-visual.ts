export type LuxuryProductVisualProps = {
  /** Tailwind width classes, e.g. `w-[min(78vw,320px)]` */
  className?: string;
  /** When true, skip continuous motion for accessibility */
  reducedMotion?: boolean;
};
