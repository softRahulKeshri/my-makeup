export type ProductJarShowcaseProps = {
  /** Tailwind width classes, e.g. `w-[min(72vw,300px)]` */
  widthClassName: string;
  sizes: string;
  priority?: boolean;
  /** Screen-reader label */
  alt?: string;
};
